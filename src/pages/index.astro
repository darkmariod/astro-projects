---
import Hero from "../components/Hero.astro";
import About from "../components/About.astro";
import Services from "../components/Services.astro";
import Contact from "../components/Contact.astro";
import GetInTouch from "../components/GetInTouch.astro";   
import Footer from "../components/Footer.astro"; 

import "../styles/global.css"; 
---

<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Pablo Rivadeneira 👨‍⚖️</title>

    <!-- Script para desplazamiento suave -->
    <script>
      function scrollToSection(event: { preventDefault: () => void; }, sectionId: string) {
        event.preventDefault(); 
        const section = document.getElementById(sectionId);
        if (section) {
          section.scrollIntoView({ behavior: 'smooth' });
        }
      }
    </script>
  </head>
  <body class="bg-gray-50">

    <!-- Hero Section -->
    <Hero />

    <!-- Main Content -->
    <main class="pt-20">
      <!-- Secciones con ids correctos -->
      <section id="about" class="pt-16">
        <About />
      </section>

      <section id="services" class="pt-16">
        <Services />
      </section>

      <section id="contact" class="pt-16">
        <Contact />
      </section>

      <!-- Sección Get in Touch con Calendly -->
      <GetInTouch />

    </main>

    <!-- Footer Section -->
    <Footer />
    
  </body>
</html>
